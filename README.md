# Vistula Java Project – Task 1

Educational **Spring Boot** project demonstrating the basics of controllers, templates, and static resources (images).

---

## 📌 Project Description

This project implements:
- a simple Spring MVC controller
- a greeting page with request parameters
- usage of static resources (images)
- basic Spring Boot application structure

---

## 🚀 How to Run the Project

1. Make sure **JDK** and **Maven** are installed
2. Run the application using the command:

```bash
mvn spring-boot:run
```
or run the main class from your IDE (e.g. IntelliJ IDEA)
## 🖼 Greeting

```bash
curl --silent http://localhost:8080/greeting?name=Alex
```
